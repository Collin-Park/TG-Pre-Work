const vacationSpots = ['Bali', 'Paris', 'Tulum'];

// Write your code below

for (let i = 0; i< vacationSpots.length; i++) {
  console.log(vacationSpots[i])
};

/*we can loop through the array by using .length to find the max value. while looping
through we can console.log the elements in the array*/
