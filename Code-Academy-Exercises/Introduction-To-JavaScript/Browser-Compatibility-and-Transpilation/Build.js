/*
once everything is set up correctly, we can run the code in bash: npm run build to transpile
all the code in the directory written in ES6 or above, and transpile them into ES5 and saves
them to the directory named lib
*/
