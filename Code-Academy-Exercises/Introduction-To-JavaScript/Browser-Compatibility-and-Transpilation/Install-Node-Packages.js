/*here we learned to install node packaged using bash,
-cli include command line tools for all ES 6+ down to ES 5,
-D stands for devDependencies, it means other developers can run the project without
needing to install dependencies. */
