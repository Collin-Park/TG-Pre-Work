const entree = 'Enchiladas';
console.log(entree);
entree = 'Tacos'

/*
const can be called similarly to var and let. however as the opposite of let,
const doesnt allow the variable defined by it to be redefined as we saw in let.
a TypeError will ensure when you try.
*/
