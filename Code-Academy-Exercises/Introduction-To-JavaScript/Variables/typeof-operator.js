let newVariable = 'Playing around with typeof.';

console.log(typeof newVariable);

newVariable = 1;

console.log(typeof newVariable);

/*we can ascertain the typeof variables, by using the typeof operator. */
