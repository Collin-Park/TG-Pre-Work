let hungerLevel = 7;

if (hungerLevel >= 7) {
  console.log('Time to eat!')
} else {
  console.log('We can eat later!')
}

/*
comparison operators are <, <= , >, >= and ===.
these are operators that compare the left and right of the operator to return a
boolean.
*/
