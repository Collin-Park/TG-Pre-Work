let sale = true;

sale = false;

if(sale) {
  console.log('Time to buy!');
} else {
  console.log('Time to wait for a sale.')
};

/*
an else statment must be paired with an if statement, it signals that if not true,
perform the block command in the else block {} 
*/
