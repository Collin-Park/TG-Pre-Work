let sale = true;
sale = false;

if (sale) {
  console.log('Time to buy!')
};

  /*
an if statement is followed by () if the condition inside the () returns true then
{} or the block stament should operate.
if the condition is false in the parens, the nested block statement will not operate
  */
