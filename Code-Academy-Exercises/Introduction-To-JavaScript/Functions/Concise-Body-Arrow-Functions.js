const plantNeedsWater = day => day === 'Wednesday' ? true : false;

/*function can be made more concise by using a single-line block of code
we also use the ternary operator ? to replace the if statement and : after the ?
to signal else to signal false after the colon.*/
