const chores = ['wash dishes', 'do laundry', 'take out trash', 'cook dinner', 'mop floor'];


chores.pop('cook dinner', 'mop floor');

console.log(chores);

/*.pop removes items from the list. it searches for a hit between the elements
in the list and the arguments in the input and removes the match from the list.*/
