var logVisibleLightWaves = () => {
 const lightWaves = 'Moonlight'
 console.log(lightWaves)
}
logVisibleLightWaves()
console.log(lightWaves)

/*if a variable or a function is defined inside a block, it is not global and is
only defined within the block. this might be useful for security issues, or to create
temporary variables that dont need to be called globally 
when we try to call for the variable defined within the block scope
outside of the scope we run into an error.*/
